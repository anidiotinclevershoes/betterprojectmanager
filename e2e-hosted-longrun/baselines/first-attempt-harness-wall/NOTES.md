# First production attempt — harness wall (not a Lume stop)

Run `lr-20260912T2155Z` against production `https://betterprojectmanager.vercel.app`.

Isolation passed. State 0 created dedicated project `469539ae-599f-4bc1-840e-26116d90d023` with all five people, both issues, three todos, three dates. Only the “snag lists” knowledge sentence was missing from Organise.

Capture 1 analysed (live OpenAI) and produced **no** `article[data-review-family]` cards. Knowledge-only observations may have landed in Remember for later, which this harness did not collect. Apply count 0.

After Analyse, compose `ocean-capture-input` is unmounted (annotated transcript). Captures 2+ failed at `openCapture` — **harness**, not product. No further canonical writes from C2+.

Frozen manifest was not changed. Harness now clicks **New Capture** between captures and Remember-matches expected Knowledge.

Official first complete run uses a new run id on a new E2E-LONGRUN project.
