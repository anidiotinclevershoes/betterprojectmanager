# Hosted vertical — calibrated baseline hv-cal-20260911T194000Z

Target: same PR #155 Preview. Harness: `cursor/hosted-vertical-journeys-2024` at `bbb8061`.
Independent Bob/Mike: `hv-cal-bob-20260911T193311Z` PASS.

No production changes in this pass. EntitlementGate remount remains fixed.

## Original run hv-20260911T185955Z (reclassified)

See `pr-155-hv-20260911T185955Z-reclassified.md`.

## Calibrated matrix

| Journey | Result | Classification | Earliest real product boundary |
|---|---|---|---|
| New Project partial people | PASS | — | none (remount journey PASS; 401 was AUTH_FLAKE) |
| Full New Project + Create + hard reload | PASS | — | none (draft ISO dates + compact `12 Sep` / `15 Sep` persist) |
| Capture date update → Apply → reload | FAIL | PRODUCT_DEFECT | PROJECTION_RELOAD (Apply 200 / Review showed 20 Sep; hard reload paints `Production release · 12 Sep`) |
| New person / responsibility | PASS | — | none (Andris `needs_you`) |
| Ambiguity stays local | FAIL | PRODUCT_DEFECT | REVIEW_UI (pronoun isolated as Needs You; date sibling also Needs You / unmatched, 0 Apply-ready). Prior iteration also hit APPLY `persist_milestone_create_with_receipt` missing from hosted schema cache when create was offered. |
| Mixed realistic paste | FAIL | PRODUCT_DEFECT | PROJECTION_RELOAD (semantic Review + Apply 200; hard reload still `Production release · 12 Sep`) |

Runtime: 87s. Live OpenAI `provider=openai` `fallback=false` on every Organise/Capture call.

## Totals

REAL PRODUCT FAILURES: 3
HARNESS/AUTH/SETUP FAILURES: 0 (on this calibrated run)

Original-run harness/auth/setup that this pass corrected (not product): Bob 401 AUTH_FLAKE; Full NP compact-date regex; Capture compact `20 Sep` regex; Ambiguity EAH code collision; Mixed exact-sentence card regex.

## Empty-server (D-052) — separate, not fixed

PR #157 / `cursor/np-organise-observation-loss-cedc`. VALIDATE drops schema-rejected observations before `draftFromProvisional`. Ambiguity no longer calls Organise, so this baseline does not re-hit empty `draft.stakeholders`. Do not implement the adapter fix here.

## What genuinely proves UI → hosted API → live OpenAI → Review → Apply → reload

None of the six journeys on `hv-cal-20260911T194000Z` complete that full Capture path.

- Full New Project proves Organise → Create → hard reload (not Capture Review/Apply).
- Capture date and Mixed prove through Review + Apply HTTP 200, then lose the new date on hard reload.
- An intermediate Mixed run (`hv-cal-20260911T193400Z`) did pass reload once; the clean rerun reverted to `12 Sep`. Apply→reload is not stable.

Do not start the PROJECTION_RELOAD or Ambiguity Review/Apply product fixes in this calibration pass.
