# AI-first Capture Gate 2.5

Non-mergeable bounded hardening of Gate 2.

```bash
LUME_EXPERIMENT=1 npx --yes tsx scripts/experiment-ai-first-capture-gate2-5.ts
```

Allowed only: exact same-value → no_change; explicit current-project header;
atomic person+responsibility contract sentence.

Does not import Simplified Capture. Does not change production Capture.
Does not Apply.
