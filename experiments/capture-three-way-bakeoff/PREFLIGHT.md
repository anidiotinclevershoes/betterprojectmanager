# Three-way bake-off preflight

Recorded before corpus freeze and live runs.

```text
Working branch: experiment/capture-three-way-bakeoff
Branch HEAD at creation: 920d65d9c0f6e49efca8e26fc8906dbbd11e8f25
origin/main HEAD: 920d65d9c0f6e49efca8e26fc8906dbbd11e8f25
Merge-base: 920d65d9c0f6e49efca8e26fc8906dbbd11e8f25
Ahead: experiment-only files
Behind: 0 at branch creation
Contains current main?: YES (experiment branch created from current main)
Working tree at preflight: clean origin/main, then experiment-only additions
PR base: none — non-mergeable experiment
Dependencies: none owned in production Capture
Shared/global files expected: none
Branch classification: EXPERIMENT (LUME_EXPERIMENT=1)
```

## Pins

| Contender | SHA | Available | Isolation |
| --- | --- | --- | --- |
| Current | `920d65d9c0f6e49efca8e26fc8906dbbd11e8f25` | YES (equals origin/main) | worktree `/tmp/lume-bakeoff/current` |
| Simplification | `bd76bbf35b60cec2685e9bb106a4d4a4a9dbdab0` | YES (`integration/capture-simplification-v1`) | worktree `/tmp/lume-bakeoff/simplification` |
| AI-first | `707b704bbf820fcc4492c86155889d6afe5d5bae` | YES (`experiment/ai-first-capture-gate1-v2`) | worktree `/tmp/lume-bakeoff/ai-first` |

Simplification contains current `origin/main` (`920d65d`) plus rescue-removal. It is **not** on `main`.

AI-first Gate 1 v2 does **not** contain current `origin/main` (branched from pre-#175 `92d61d1`). That is intentional: the accepted Gate 1 architecture is pinned, not rebased onto later Capture changes.

## AI-first candidate selection

Known SHAs:

| SHA | Branch | What changed | Verdict |
| --- | --- | --- | --- |
| `56d9148` | `experiment/ai-first-capture-gate1` | 5-case Astra spike | PROMISING. Stop for human review. Gate 2 not started. |
| `707b704` | `experiment/ai-first-capture-gate1-v2` | 12-case Astra vs production; rematerialise/hydrate bypassed | AI-FIRST PROMISING WITH CONDITIONS. Do not proceed automatically to Gate 2. |

Accepted comparison candidate: **`707b704`**.

Why this is not a silent recency pick:

- Gate 1 v2 is the expanded semantic comparison, not a retune of Gate 1.
- `experiment/ai-first-capture-gate2` PREFLIGHT explicitly names Gate 1 v2 HEAD `707b704` as its base.
- This bake-off uses Gate 1 architecture only (raw Capture + dumb snapshot + one Astra call). Gate 2 legal-write boundary is **not** used.

## Models

- Current / Simplification: `gpt-4o-mini-2024-07-18` via `resolveOpenAIChatModel()`. Probe: available.
- AI-first: `gpt-6-astra` (`GATE1_V2_DEFAULT_MODEL`). Probe: available. No silent substitution.
- `OPENAI_API_KEY`: present.

## Hosted environment

`LUME_E2E_*` is present, but the preview host is a **different** Vercel git SHA (`cursor-vis-c19eaf`), not the pinned Current or Simplification checkouts.

Stage 3 therefore uses each pin's **in-memory production Apply path** in isolated worktrees (disposable worlds). AI-first is excluded from Apply.

Running Simplification against the hosted preview would not be Simplification.

## Existing fixtures used (not redesigned after seeing outputs)

- `src/lib/eval-capture-v2/corpus.ts` live cases
- Gate 1 v2 / Gate 2 fixture capture texts and fixture expected facts
- Simplification observe journeys
- Hosted holdout H6
- Long-haul C5 timber-floor (regression intent + reconstructed Riverside seed)
- Harbourline stress identity / competing-date cases

Expected outcomes are encoded in `FROZEN_CORPUS.json` with `expectedSource` labels. They were not generated by a contender or by an LLM.

## Isolation safety

- No merge / cherry-pick / rebase among the three pins.
- Harness copies an untracked `.bakeoff-runner.ts` into each worktree at run time.
- Normalization copies present fields only; missing fields are `omitted`.
