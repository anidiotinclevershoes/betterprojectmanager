# Test-only dashboard fixtures

These JSON files are **fake**. They exist so the reporter can be tested without
provider calls. Never publish them to the live Lume Test Dashboard issue.
Never treat them as Capture V2 benchmark history.
