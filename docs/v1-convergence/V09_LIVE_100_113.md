# LUME v0.9 LIVE 100 — FINAL QUALIFICATION (PR #113)

**TEST / RELEASE QUALIFICATION ONLY. Production was not modified. Do not merge.**

Candidate production: PR **#113** (`cursor/v09-ready-apply-parity-9524` @ `1919c74`).

This branch: `cursor/v1-v09-live-100-113-610b`.

Scenario: unchanged Northstar 100 from PR #108 / #112. Oracle expected truth is not edited.

```bash
npm run stress:project-longhaul -- --captures=100 --mode=live --out=longhaul-100-live
```

Live mode requires real OpenAI + disposable Supabase. It does **not** fall back to oracle envelopes, regex Capture, `localCaptureFallback`, or `FakeWorkspaceClient`.

Cursor VM is blocked for the live run. CI (`.github/workflows/v09-live-100.yml`) is the live path.

The filled report is written after the live job completes. See `longhaul-100-live/`.
