# LUME v0.9 FINAL LIVE 100

**TEST / RELEASE QUALIFICATION ONLY. Production was not modified. Do not merge.**

| | |
| --- | --- |
| Candidate | PR **#116** `cursor/v09-shared-seam-hardening-9524` @ `d5f5d63` |
| Harness | PR **#117** `cursor/v1-v09-live-100-116-610b` @ `55f7def` (CI merge SHA `e027f66`) |
| CI | [run 33338199211](https://github.com/anidiotinclevershoes/betterprojectmanager/actions/runs/33338199211) **live-100 SUCCESS** (~7 min extract/apply after Supabase start). All 6 checks green. |
| Mode | **live** `gpt-4o-mini-2024-07-18` + disposable local Supabase |
| Fake / envelopes / observation repair | **false** |
| Scenario / oracle | unchanged Northstar 100 from #108 / #112 / #114 |
| Captures | **100 / 100**. `stoppedAt` null. Reload OK at C1 / C10 / C25 / C50 / C75 / C100. |
| AI calls | 100 extract + 30 Ask + 12 analyse-only |

Oracle `divergenceCount: 100` is **not** the verdict. The harness auto-approves Ready items. Classification below uses the **real product Review gate**.

Evidence: `longhaul-100-live/` on this branch.

---

## Executive verdict

**READY FOR v0.9 RELEASE**

#116’s shared-seam pass holds under live extract.

The #113 / #114 P0 is gone. Model `obs-1` / `obs-2` are reused on almost every capture. Apply never treated a later create as “already applied.” C11 is the exhibit: UAT, CAB, and Release all wrote.

Ready means Apply executed. 73 Ready items, 73 writes, 0 receipt-replay drops, 0 thrown Apply, 0 wrong-project writes.

The automated scorer still marks **A = 3**. Under the Review safety model those three are **not** hidden post-Review corruption. Review would have shown the exact proposed create or the exact ISO date, and Apply saved that proposal. B is zero (no person merge, no split-brain identity). C42 “Sarah said…” stayed Needs You.

Ask no longer promotes knowledge/history prose to `direct_confirmation` for current date, owner, or open risk.

Remaining residue is model quality behind a working Review gate: missing kickoff people (model said `update_existing` on a todo id → Needs You), and year-shaped ISO dates (`2023-10-18` / `2023-11-18`) that Review would display.

Do not merge this harness PR. The production candidate is #116.

---

## Ready→Apply

| | |
| --- | ---: |
| Resolve-Ready items Applied | **73** |
| `executed.kind === "wrote"` | **73** |
| Non-effects | **0** |
| Receipt-replay `no_change` | **0** |
| Legitimate `no_change` after Ready | **0** |
| Thrown exceptions | **0** |
| Wrong-project blocks | **0** |
| Apply `needs_you` / `failed` after Ready | **0** |

**Systemic Ready-but-unexecutable gap: no.**

#114 on the same scenario: 77 Ready, 38 wrote, 39 `already applied`. That storm is gone.

---

## Receipt identity

Live extract reused model-local ids exactly as before:

| Model id | Ready uses | Distinct system `suggestionId`s |
| --- | ---: | ---: |
| `obs-1` | 43 | 43 |
| `obs-2` | 19 | 19 |
| `obs-3` | 7 | 7 |
| `obs-4` | 2 | 2 |
| `obs-5` | 2 | 2 |

All 73 suggestion ids are UUIDs from `newReviewOperationId()`. None are `v2-obs-*`. Zero id collisions.

**C9 then C11 (the #114 exhibit):**

| Capture | Model id | Review content | Apply |
| --- | --- | --- | --- |
| C9 | `obs-1` | Login error handling todo | **wrote** `a3c7f90f-…` |
| C11 | `obs-1` | UAT Start / 2026-10-14 | **wrote** `6c0b62a7-…` |
| C11 | `obs-2` | CAB Date / 2026-10-18 | **wrote** `b35f94aa-…` |
| C11 | `obs-3` | Target Release / 2026-10-27 | **wrote** `bfaee811-…` |

Unrelated operations were not dropped as already applied.

---

## Hidden unsafe truth failures — A

**Hidden post-Review A: 0.**

Automated scorer A = 3. Each case, per the required four questions:

### C45 — acronym restatement of the SSO todo

1. **Model proposed:** two `create_new` risks, titles `SSO IdP MFA SLO timing risk` and `IdP regression impact on CAB`.
2. **Review would show:** two Ready risk creates with those exact titles.
3. **User could reject/correct:** yes.
4. **Apply saved:** exactly those two risks.

Expected: silent restatement of the existing SSO todo. Extra risks are model over-extraction. **MODEL/REVIEW-GATED QUALITY (F), not hidden A.**

### C71 — “CAB is still the 18th”

1. **Model proposed:** `update_existing` CAB Date → ISO `2023-11-18`, `truthIntent: current`. Commentary correctly `non_current` for the mistaken 22nd.
2. **Review would show:** milestone update, label CAB Date, date **2023-11-18**.
3. **User could reject/correct:** yes. The consequential value is the ISO on the Review card.
4. **Apply saved:** CAB `startAt = 2023-11-18T12:00:00.000Z`.

Expected: no-change (stay 18 October **2026**). The year is a model completion error. Same class as #114 C71/C82. **MODEL/REVIEW-GATED QUALITY (E), not hidden A.**

### C95 — steering recap “export encoding still open”

1. **Model proposed:** `create_new` todo `export encoding defect` (other recap lines correctly `no_change`).
2. **Review would show:** create todo “export encoding defect”.
3. **User could reject:** yes.
4. **Apply saved:** a second row titled `export encoding defect` (checkpoint duplicate count 2 at C100).

Not a silent mutation of an existing id. Not a person merge. **MODEL/REVIEW-GATED extra create (F).** Duplicate *title* residue is P2, not B.

---

## Identity / duplicate corruption — B

**B = 0.**

- No duplicate person names.
- Sarah Kim created on C33 beside the Sarah Okonkwo *mention*; not merged into anyone.
- C42 “Sarah said the CAB pack needs a threat model” → Needs You (responsibility). Did not bind the wrong Sarah.
- C51 “Sarah said the member comms…” → Needs You.
- C43 responsibility for Sarah Okonkwo (who was never persisted) did **not** become Ready; only the threat-model todo wrote.
- Neighbour Atlas untouched. Reload equal at every checkpoint.

---

## Review-gated model errors

Visible on Review; Apply executed the reviewed fields.

| Cap | Review would show | Apply |
| --- | --- | --- |
| C64 | UAT Start → `2023-10-18` | wrote that ISO |
| C71 | CAB Date → `2023-11-18` | wrote that ISO |
| C45 | two new open risks | wrote both |
| C95 | create “export encoding defect” | second row |

truthIntent on C73 / C76 / C77 was `current` on `no_change` restatements (scorer E). None wrote. Safe.

C82 proposed another CAB `2023-11-18` update and stayed `no_change` (same day already stored).

---

## Needs You quality

**C = 22.** Allowed and expected.

Kickoff C1–C5: model used `update_existing` targeted at the **todo** “Confirm project baseline with key stakeholders” for Priya / Liam / Sarah Okonkwo / Marcus / Elena. Person identity gate refused. Truth stayed empty rather than inventing stakeholders. That is conservative and correct.

C42 / C51 first-name “Sarah said…” stayed Needs You.

Later Priya / Liam / Sarah Okonkwo mentions were mostly `no_change` (C55 attendees). They never became rows. That is a model miss after a safe refusal, not a bypass.

---

## Extraction misses

Automated **D = 0** (every expected write had a covering observation string). Real misses sit in C / G:

- Priya Shah, Liam Brooks, Sarah Okonkwo, Marcus Chen, Elena Voss never persisted.
- G = 8 oracle-domain mismatches (person extracted as responsibility, todo as knowledge, etc.). Those either Needs You or wrote a different safe domain. Not hidden A.

Analyse restatements (Priya still PM, release still 27 Oct, CAB rejection historical) produced **zero writes**.

---

## truthIntent

C11 dates were `current` with correct 2026 ISOs and wrote.

Stale / rejected-date captures:

| Cap | Expected intent | Got | Wrote? |
| --- | --- | --- | --- |
| C71 | `non_current` | `current` + commentary `non_current` | yes — reviewed 2023-11-18 |
| C73 | `non_current` | `current` + `uncertain` | no (`no_change`) |
| C76 | `non_current` | `current` | no |
| C77 | `non_current` | `current` | no |

Imperfect truthIntent is not a blocker when Review shows the date and Apply matches Review.

---

## Ask authority

30 live `answerTellMeQuestion` probes at six checkpoints.

| Question | Authority required | What happened |
| --- | --- | --- |
| Current target release date | timeline / release | `not_found` until dates exist; then **`related_context`** stating `2026-10-27`. Never `direct_confirmation` from knowledge. |
| Main open risks | risk rows | **`related_context`**, listing actual risk titles (or none early). Never `direct_confirmation` from prose. |
| Who currently owns UAT | confirmed responsibility | **`not_found`** at every checkpoint (no UAT owner row — Priya never landed). Correct. |
| Important open actions | todos | `direct_confirmation` listing board todo titles. |
| Why did CAB/release move? (historical) | history allowed | Answers from current/history narrative. C75 mentions the written `2023-11-18`. C100 says CAB is still 18 October 2026 (board is 2023-11-18) — model wording vs board, not a knowledge-as-first-class current-date claim. |

Current date / owner / risk were **not** labelled `direct_confirmation` from knowledge or history. The #116 source-kind gate held. Conservative `related_context` on a real timeline/risk row is P2, not a blocker.

New Project was **not** exercised by this Capture-only qualification. No New Project blocker is claimed.

---

## Final project coherence

| Domain | Landed | Notes |
| --- | --- | --- |
| Release | **2026-10-27** | Correct. Never moved. |
| UAT Start | 2023-10-18 | Review-gated C64 year. |
| CAB Date | 2023-11-18 | Review-gated C71 year. |
| People | 7 | Jordan Hale, Dev Patel, Amira Rahman, Tomiko Sato, Chris Bell, Sarah Kim, Nadia Qureshi. Missing Priya / Liam / Sarah Okonkwo / Marcus / Elena (Needs You on bad `update_existing`). |
| Todos | 26 | Core work present (SSO, UAT script, CAB pack, defects, hypercare). One duplicate title (`export encoding defect`). |
| Risks | 6 | API timeout, vendor delay, legacy session, plus C45 extras and pen-test watch. |
| History | 74 | A History event after each write. |
| Isolation | Atlas clean | Reload OK. |

The board is a real project, not the empty #112 board and not the #114 receipt-starved board. Kickoff people and two dates would have needed a human Review correction.

---

## Token/cost growth

| | Early (1–20) | Late (81–100) | Ratio |
| --- | ---: | ---: | ---: |
| Capture request chars | 4730 | 7276 | **1.54** |
| Estimated input tokens | 1124 | 2196 | **1.95** |
| Project-block chars | 757 | 3273 | 4.32 |
| Current truth objects | 12.3 | 48.5 | 3.94 |
| History events | 9.85 | 59.5 | 6.04 |

Ask context early→late chars 2.27×. Provider extract tokens mean 1849 in / 323 out. Growth is history-shaped, not runaway. 100 captures completed.

---

## Remaining P0

**NONE.**

## Remaining P1

**NONE.**

No hidden Review≠Apply. No receipt collision. No person merge. No wrong-project write. Ask does not mint current date/owner/risk `direct_confirmation` from prose.

## Remaining P2

- Kickoff people: model `update_existing` on a todo id → Needs You; Priya / Liam / Sarah Okonkwo never recovered.
- Year-less “the 18th” completed as `2023-*-18` and Review-approved by the harness (C64, C71).
- Extra risks on an SSO acronym restatement (C45).
- Second `export encoding defect` todo (C95).
- Ask `related_context` (not `direct_confirmation`) even when a timeline/risk row exists — conservative.
- C100 historical Ask wording vs the written CAB ISO.
- Oracle G domain mismatches; oracle `divergenceCount` still 100 (not a metric).

---

## Recommendation

**READY FOR v0.9 RELEASE**

The structural receipt / Ready→Apply / Ask-prose-authority failures that blocked #113 are not present on #116.

Ship #116. Do not merge #117 (harness only). Do not treat oracle divergence or Needs You volume as a release fail.
