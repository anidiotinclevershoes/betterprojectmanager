# LUME v0.9 LIVE 100-CAPTURE QUALIFICATION

**TEST / RELEASE QUALIFICATION ONLY. Production was not modified. Do not merge.**

Canonical copy of the filled report: `longhaul-100-live/report.md`.

- Candidate production: PR **#110** (`cursor/v09-shared-truth-hardening-9524` @ `95ac368`)
- This branch: `cursor/v1-v09-live-100-610b` (PR **#112**)
- CI: [live-100 run 33307350137](https://github.com/anidiotinclevershoes/betterprojectmanager/actions/runs/33307350137) **SUCCESS**
- Scenario: unchanged Northstar 100 from PR **#108**

```bash
npm run stress:project-longhaul -- --captures=100 --mode=live --out=longhaul-100-live
```

Live mode requires real OpenAI + disposable Supabase. It does **not** fall back to oracle envelopes, regex Capture, `localCaptureFallback`, or `FakeWorkspaceClient`.

---

## Recommendation

**STRUCTURAL PROBLEM REAPPEARED**

100/100 live captures completed. People stayed `[]`. Apply failed to persist 37 Ready captures. Current truth at C100 is not a maintained project.

Do not merge #108, #110, #111, or #112. Production changes from this PR: **NONE**.

DB-boundary (#111) remains independently QUALIFIED and is not reopened here.

---

The 15 sections follow.

---

## 1. Executive verdict

**STRUCTURAL PROBLEM REAPPEARED**

The live 100 completed (100/100 captures, 130 counted AI calls + 12 analyse-only extracts, no safety stop). Persistence reloaded at every checkpoint. Atlas was not mutated. That is not enough.

After 100 live Captures, Northstar is **not a maintained project**. People = `[]`. The one milestone is a CAB sentence whose date wandered 18 → 16 → 20 → 18. The one risk is a 24-hour CAB-pack *rule* stored as a risk. To-do titles are still capture prose. Identity only bound four keys, and three of those are sentence-titles.

| Axis | Verdict | Why |
| --- | --- | --- |
| Truth longevity | **RED** | 0 people for the entire run. Priya never created. One milestone absorbed UAT/CAB/release. Current board is not the project the 100 updates described. |
| Capture extract quality | **RED** *(LIVE-PROVEN)* | C1 bound Priya to the seed to-do. 15 `person/create_new` rows omitted `proposedValues.name`. `truthIntent` almost never emitted `non_current` (6/229). A=5, B=1, E=6. |
| Apply / persist | **RED** *(LIVE-PROVEN)* | 56 captures produced Ready write rows; only 23 persisted anything. **G=37** Ready applies returned `executed.kind !== "wrote"` with **zero thrown exceptions**. |
| Ask honesty | **AMBER** *(LIVE-PROVEN)* | `not_found` is decent when the board is empty. Late Ask invents a 27 October release with `direct_confirmation` and invents SSO/Jordan reasons from thin History. |
| Token / cost growth | **AMBER** *(LIVE-PROVEN, misleadingly small)* | Extract prompt 2.8k → 3.2k chars (1.13×). Growth is small **because current truth never filled in**. Not a quality win. |
| Persistence / reload / isolation | **GREEN** | 100/100 completed. Reload matched at CK1/10/25/50/75/100. Atlas Quinn Adler + ledger to-do untouched. `stoppedAt` null. |

Oracle `divergenceCount: 100` is **not** the release metric (string compare vs the #108 expected world). The release metric is the A–G taxonomy.

**Do not treat this as a v0.9 release candidate.**

---

## 2. Run environment

| | |
| --- | --- |
| Production candidate | PR #110 `cursor/v09-shared-truth-hardening-9524` @ `95ac368` |
| Harness branch | `cursor/v1-v09-live-100-610b` @ `253eaa3591db2d0ee3f8c6ca105b5355e208d820` |
| CI live-100 | [run 33307350137](https://github.com/anidiotinclevershoes/betterprojectmanager/actions/runs/33307350137) **SUCCESS** (~8 min) |
| Artefact `harnessSha` | `2c099e467e81ba10a9a666f42130b7ebdf19d04b` (Actions `pull_request` merge commit; not on the branch tip) |
| mode | **live** |
| model | `gpt-4o-mini-2024-07-18` |
| persist | disposable local Supabase (`supabase start` on `ubuntu-latest`) |
| FakeWorkspaceClient | **false** |
| oracle envelopes / regex Capture / `localCaptureFallback` | **false** |
| captures attempted / completed | **100 / 100** |
| live extract calls | 100 |
| Ask calls | 30 (5 probes × 6 checkpoints) |
| Analyse-only probes | 12 (never Applied) |
| total AI calls (summary field) | 130 (= extract + Ask; analyse extra) |
| provider calls including analyse | **142** |
| elapsed | 354417 ms |
| stoppedAt | `null` |
| firstDivergence | C1 `people.person:priya-shah: expected Priya Shah actual (missing)` |
| Cursor VM | blocked (no `OPENAI_API_KEY`, no Docker). CI was the live path. |

Proof labels:

- Persistence / Apply / History / reload — **LIVE-PROVEN** disposable Supabase
- Extract quality — **LIVE-PROVEN** `extractObservationsWithOpenAI`
- Ask answers — **LIVE-PROVEN** `answerTellMeQuestion`
- Token API usage — **LIVE-PROVEN** OpenAI `usage`
- UI pages / Playwright — **not run** on this workflow

e2e / regression / Vercel reds on PR #112 are Harbourline / #110 issues. Out of scope. The `live-100` job is the qualification job.

---

## 3. Project at Capture 100

**People: none.** Priya Shah, Liam Brooks, Sarah Okonkwo, Sarah Kim, Marcus Chen, Elena Voss, Jordan Hale, and everyone else are missing. Atlas still has Quinn Adler.

**Dates:** one timeline row, label `CAB is scheduled for 18 October 2026.`, `startAt` 18 October 2026. There is no UAT-start row and no Release row. The same CAB id absorbed later “UAT moves to 20th / 16th” writes.

**Open work (5):** seed “Confirm project baseline…”; “A to-do for login error handling needs to be added.”; vendor-contract sentence for Liam; “Member communications are still due on 26 Oct.”; “Password-reset regression is done.” (`done: false`).

**Risks (1):** `There are no exceptions to the rule about CAB packs.` status `watch`.

**Knowledge / decisions that did land:** feature-flag cutover; SSO in / marketing widgets out; weekend cutover rejected; “go on 27 October”; member comms day-before-release (C51 unsafe Ready).

Identity map bound only `todo:login-error-handling`, `milestone:uat-start` (the CAB-labelled row), `todo:vendor-contract-check`, `todo:feature-flag-cleanup`.

---

## 4. Truth integrity

Reload OK at every checkpoint. Atlas untouched. No duplicate people (there were none). History 1 → 26 after writes only.

**Person-create contract (structural).** Resolve refuses `person/create_new` unless `proposedValues.name` or `candidateTargetTitle` is a usable name. The extract schema lists `proposedValues` as `{ status, date, ownershipSemantics }` — no `name`. All 15 live `person/create_new` rows had empty/null `proposedValues`. C1 bound Priya as `update_existing` on the seed to-do.

**Apply after Ready (G).** 37 captures: resolve said write; `applyApprovedCaptureSuggestion` did not return `executed.kind === "wrote"`. Zero thrown `applyError`. 33 persisted nothing; 4 were partial (C11: 3 milestone creates, 1 wrote).

Mean 2.29 observations/capture (min 1, max 9). `truthIntent`: 201 current / 22 uncertain / 6 non_current.

---

## 5. Truth checkpoint table

| CK | People | Todos | Risks | Milestone label / date | History | Reload |
| ---: | ---: | ---: | ---: | --- | ---: | --- |
| 1 | 0 | 1 (seed) | 0 | — | 1 | OK |
| 10 | 0 | 2 | 1 (CAB-pack rule, open) | — | 4 | OK |
| 25 | 0 | 2 | 1 | CAB sentence / 18 Oct | 6 | OK |
| 50 | 0 | 3 | 1 | CAB sentence / **16 Oct** | 14 | OK |
| 75 | 0 | 3 | 1 (watch) | CAB sentence / **20 Oct** | 20 | OK |
| 100 | 0 | 5 | 1 (watch) | CAB sentence / **18 Oct** | 26 | OK |

Search at C100: `Priya Shah` 0; `UAT` 0; `API timeout` 0; `CAB` 3; `feature flags` 2.

---

## 6. Capture quality over time

| Letter | n | Meaning |
| --- | ---: | --- |
| **A** | **5** | Silent / no-change capture wrote current truth (30, 46, 76, 78, 95) |
| **B** | **1** | Unsafe Ready on ambiguous capture (**C51** Sarah / member comms) |
| C | 24 | Conservative Needs You (includes C1–C4 people and correct C42) |
| D | 14 | Expected fact never observed |
| **E** | **6** | Wrong `truthIntent` (71, 73, 76, 77, 78, 82). C63 scored correct. |
| F | 4 | Extra create / wording |
| **G** | **37** | Apply failed after Ready |
| correct | 25 | |

C42 (“Sarah said… threat model”) correctly Needs You. C51 (“Sarah said… member comms”) Ready-wrote. Same first-name trap.

Dated/stale (existing #108 captures; Part-2 phrases were not added to the scenario): 76 and 78 wrote current truth (A+E). 71/73/77/82 stayed Needs You or no_change (safe, wrong intent).

---

## 7. Capture token / context / cost growth

**LIVE-PROVEN** OpenAI `usage`.

| | Captures 1–20 | Captures 81–100 | Ratio |
| --- | ---: | ---: | ---: |
| Extract prompt chars | 2823 | 3182 | **1.13×** |
| tiktoken input | 639 | 769 | **1.20×** |
| projectBlock chars | 454 | 784 | **1.73×** |
| current truth objects | 6.1 | 18.75 | **3.07×** |
| History events | 3.5 | 23.75 | **6.79×** |

Provider extract tokens: input median 768 / max 888; output median 270 / max 837; total mean 1037.

History is **not** in the V2 extract prompt. Growth is small because current truth never filled in. Deterministic #108 grew 3.8× / 5.3× after oracle Apply filled transcript titles. Do not cite live 1.13× as a cost win.

---

## 8. Ask quality + token/cost growth

30 live Ask calls. Ask did not mutate truth.

- UAT owner: `not_found` at every checkpoint (correct given 0 people).
- Open risks: from CK10 onward, the malformed CAB-pack-rule risk.
- Release date: CK50 claims 18 Oct (`direct_confirmation`); CK100 claims 27 October (`direct_confirmation`) from a decision row, not a milestone.
- Why-moved: CK50/CK75 invent SSO/Jordan causal stories from the wandering CAB row.

Ask context 1354 → 2727 chars (2.01×) early vs late. Historical Ask larger than current-state Ask. Canonical current Ask still omits History.

---

## 9. UI findings

Playwright was **not** on this workflow. Inferred from MissionState: empty People, five prose to-dos, one CAB date, one rule-as-risk. Opposite of #108’s bloated blobs; still not a usable board.

---

## 10. History behaviour

26 Northstar events. Generic titles (`Capture added a To Do`). Not in the V2 extract prompt. Not in current-state Ask. Grew 6.8× while people stayed at 0.

---

## 11. Curve-ball results

Same-first-name: C42 safe, C51 unsafe write, C33 Apply failed / Sarah Kim missing. Date moves wrote onto the only milestone. Stale/rejected dates: intent mostly wrong; 76/78 wrote. Atlas reference (C80) no_change. Analyse `repeat-release` would still write a 27 Oct milestone at CK25–100. `reaffirm-priya` stays `no_change` (vacuous). `resolved-cab-historical` would not reopen.

---

## 12. Architecture stress findings

1. Person identity cannot be born from live extract (schema vs resolve).
2. Ready ≠ persisted (G=37).
3. One CAB sentence absorbs every date.
4. Titles are still utterances.
5. `truthIntent` is mostly unused.
6. History grows; current does not. Extract cost stays flat for the wrong reason.
7. Isolation + reload hold.
8. G includes some correct Apply conservatism; the product still cannot record a named person or a second milestone.

---

## 13. 2000-CAPTURE READINESS

**No.** Longevity failed at C1. Token headroom is an empty-board artefact. Ask already invents stories at n=50. Do not scale. Do not rerun 100 hoping for a different `gpt-4o-mini` draw.

---

## 14. Findings ranked

1. People cannot be created from live extract.
2. Majority of Ready writes do not persist.
3. One CAB sentence is the entire date system.
4. Unsafe writes on silent / stale / ambiguous captures (A=5, B=1, E writes).
5. `truthIntent` almost never `non_current`.
6. Ask over-confident on a hollow board.
7. Transcript-shaped titles on the few rows that land.
8. Token growth looks healthy for the wrong reason.
9. Isolation + reload hold (necessary, not sufficient).

---

## 15. Final recommendation

**STRUCTURAL PROBLEM REAPPEARED**

Production changes: **NONE**. Do not merge.

---

## Artefacts

See `longhaul-100-live/` (copied from CI artefact `v09-live-100-evidence` on run `33307350137`).
