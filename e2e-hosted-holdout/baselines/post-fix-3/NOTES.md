# Hosted holdout after persist-authority fix (`ho-fix3-1`)

Not the first-run baseline. Frozen spec unchanged.

| Field | Value |
| --- | --- |
| Run id | `ho-fix3-1` |
| Product HEAD | `15e972b8fefee2ee3f2f72e02c331b995fae5a2f` |
| Preview | PR #164 Vercel Preview |
| Result | **4/6** |

## Persist family (closed)

H5 compose-UI Create reached Capture. The production “Project was not saved to your account” race did not reproduce.

## H5 remaining — ambiguous chair omitted

Safe depot create is Apply-ready (`CREATE` To Do, 16 Oct).

The huddle-chair observation was accepted as `NO_CHANGE` / already known (`ownershipSemantics: ambiguous`, reason “Already known — no mutation.”). Silent `NO_CHANGE` is dropped from Review, so Needs You never appears.

Excerpt: `h5-capture-excerpt.json`

## H6 remaining — independently complete dated create marked uncertain

Workshop update is Apply-ready. Void keys has title + `2026-10-16` but resolve fail-closed on `truthIntent=uncertain` (“It is unclear whether this should change current project truth.”). Review then paints unmatched “which record”.

Kwame Needs You is allowed. Speculative DHP Needs You is allowed.

Excerpt: `h6-capture-excerpt.json`
